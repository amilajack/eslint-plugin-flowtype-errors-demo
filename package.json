{
  "name": "eslint-plugin-flowtype-errors-demo",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "eslint": "^6.7.2"
  },
  "scripts": {
    "lint": "eslint . || exit 0",
    "test": "yarn lint || exit 0"
  },
  "renovate": {
    "extends": [
      "bliss"
    ]
  },
  "devDependencies": {
    "eslint-plugin-flowtype-errors": "^4.0.0",
    "flow-bin": "^0.114.0"
  }
}
